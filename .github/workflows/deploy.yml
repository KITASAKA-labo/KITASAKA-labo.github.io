name: deploy
on:
  push:
    branches:
      - [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Use Next.js latest 
        uses: actions/setup-node@v1
        with:
          node-version: 12
      - name: install
        run: |
          yarn install
      - name: build
        run: |
          yarn build
        env:
          GITHUB_PAGES: true
      - name: export
        run: |
          yarn export
          touch out/.nojekyll
      - name: deploy
        uses: peaceiris/actions-gh-pages@v2.3.1
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }} >> .env
          PUBLISH_BRANCH: gh-pages
          PUBLISH_DIR: ./out
